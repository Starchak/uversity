import React, { FC } from 'react';

import { Header, Footer } from '@components';

const Home: FC = () => {
  return (
    <div>
      <Header />
      Home
      <Footer />
    </div>
  );
};

export default Home;
