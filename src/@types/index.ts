declare global {
  type Params = {
    lng: string;
  };

  type Program = {
    id: number;
    title: string;
    description: string;
    img: string;
    countries: string[];
    color: string;
    link: string;
  };
}

export default undefined;
